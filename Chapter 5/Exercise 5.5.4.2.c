#include <stdio.h>

/*
 * Function:  main
 * ------------------
 *  for with empty body
 *
 *  returns: 4 7
 *           The first number is not less than second.5 10
 *           The first number is not less than second.8 3
 */
int main()
{
    int x, y;
    for(scanf("%d%d", &x, &y); x < y; scanf("%d%d", &x, &y) )
    printf("The first number is not less than second.");
}
